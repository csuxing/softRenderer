#include "ktx.h"
namespace SG
{
    SG::Ktx::Ktx(const std::string& name, const std::vector<uint8_t>& data, ContentType content_type):
        ImportImage{ name, {}, {} }
    {
        // auto dataBuffer = reinterpret_cast<>
    }
}
